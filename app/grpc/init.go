package grpc

func Init() {

	/*userEventRepository := onMemory.NewUserEventRepository()

	userService := service.NewUserService(userEventRepository)

	userServer := server2.NewUserServer(userService)

	listen, err := net.Listen("tcp", ":8080")
	if err != nil {
		panic(err.Error())
	}

	server := grpc.NewServer()

	user.RegisterUserServiceServer(server, &userServer)

	if err := server.Serve(listen); err != nil {
		panic(err.Error())
	}*/

}
